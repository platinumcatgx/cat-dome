<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gx1.dao.IUserDao">
    <resultMap id="User" type="User">
        <id column="id" property="id"></id>
        <result column="username" property="username"/>
        <result column="birthday" property="address"/>
        <result column="sex" property="sex"/>
        <result column="address" property="birthday"/>

        <collection property="roles" ofType="Role">
            <id column="ID" property="roleId"/>
            <result column="ROLE_NAME" property="roleName"/>
            <result column="ROLE_DESC" property="roleDesc"/>
        </collection>
    </resultMap>

    <select id="findAll" resultMap="User" >
        SELECT `user`.*	, role.ID,role.ROLE_DESC,role.ROLE_NAME
        FROM `user`
        left JOIN user_role ON `user`.id=user_role.UID
        left JOIN role ON role.ID=user_role.RID
    </select>
</mapper>